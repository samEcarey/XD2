import { css } from "styled-components";

export const varientSizeREG = css`
	padding: 0 1.5rem;
	height: 2.8rem;
	line-height: 2.8rem;
	font-size: 0.8rem;
`;

export const varientSizeXS = css`
	padding: 0 0.5rem;
	height: 1.8rem;
	line-height: 1.8rem;
	font-size: 0.5rem;
`;

export const varientSizeSM = css`
	padding: 0 1rem;
	height: 2.4rem;
	line-height: 2.4rem;
	font-size: 0.8rem;
`;

export const varientSizeMD = css`
	padding: 0 2rem;
	height: 3.8rem;
	line-height: 3.8rem;
	font-size: 1.1rem;
`;

export const varientSizeLG = css`
	padding: 0 2.5rem;
	height: 4.5rem;
	line-height: 4.5rem;
	font-size: 1.4rem;
`;

/* if (size === "xs") {
	return `;`;
} else if (size === "sm") {
	return ``;
} else if (size === "md") {
	return ``;
} else if (size === "lg") {
	return ``;
} else {
	return "";
} */
