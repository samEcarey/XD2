import styled from "styled-components";
import { WHITE_COLOR, PRIMARY_TINT } from "app/constants";

export const WorkflowStepFormStyled = styled.div`
    margin: 2rem 0;
    border: 1px solid ${PRIMARY_TINT};
    border-radius: 0.5rem;
    padding: 2rem 1rem;
    color: ${WHITE_COLOR};
`;
