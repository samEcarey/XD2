import React from 'react';
import { action } from '@storybook/addon-actions'
import { Headings } from './Headings'

export const HeadingsBasic = () => (
	<Headings/>
)

export default {
	component: Headings,
	title: 'Atoms',
}